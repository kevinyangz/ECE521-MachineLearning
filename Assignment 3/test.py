import numpy as np
weight_decay_factor=np.random.uniform(-9,-6) #Random Natural Log for weight decay
number_of_layer= np.int64(np.random.uniform(1,5.5)) #Random Natural Log for weight decay
learn_rate=np.random.uniform(-7.5,-4.5) #Random Natural Log for weight decay
dropout=np.random.choice([True, False])

